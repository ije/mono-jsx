# mono-jsx

mono-jsx is a JSX runtime that turns a server-rendered `<html>` tree into an HTTP `Response`, with optional client-side runtime pieces (signals, router, RPC, forms, and so on) bundled for the browser.

## Architecture

- **Server / SSR** — TypeScript sources at the repo root (`render.ts`, `jsx.ts`, `jsx-runtime.ts`, `setup.ts`, …) implement streaming HTML and wire up which client chunks to inject. This is what you change for markup, serialization, and response behavior.
- **Client runtime** — `runtime/*.ts` (except the generated `runtime/index.ts`) is browser-targeted code. `build.ts` minifies these modules (and selected exports from root `render.ts` / `runtime/utils.ts`) into embedded strings and **regenerates** `runtime/index.ts` with bit flags and inlined JS payloads consumed by the server renderer.

Do **not** hand-edit generated files: `runtime/index.ts`, `version.ts`, root `*.mjs`, and `bin/mono-jsx`. Run `deno task build` after changing anything the build pipeline emits.

## Project structure

```text
mono-jsx/
├── runtime/             # Browser runtime (signals, router, rpc, form, …)
│   └── index.ts         # Generated by build.ts — do not edit
├── test/                # Deno tests (*.test.tsx)
├── types/               # Public and internal .d.ts
├── build.ts             # esbuild: bundles root modules to *.mjs; emits runtime/index.ts, version.ts, bin/mono-jsx
├── index.ts             # Package entry (buildRoutes, createRPC, …)
├── jsx-runtime.ts       # jsx-runtime / jsx-dev-runtime entry (re-exported on npm)
├── jsx.ts               # Intrinsic elements / JSX factory hooks
├── render.ts            # SSR: vnode → HTML Response
├── setup.ts             # CLI `mono-jsx setup` and asset helpers
├── symbols.ts           # Internal vnode symbols
└── version.ts           # Generated from package.json — do not edit
```

## Build

Required before publishing to npm (`prepublishOnly` runs this) and after edits to `build.ts`, `runtime/*.ts`, or packaging entrypoints that feed `*.mjs`:

```bash
deno task build
```

The build writes `index.mjs`, `jsx-runtime.mjs`, `setup.mjs`, regenerates `runtime/index.ts` and `version.ts`, and refreshes `bin/mono-jsx`.

## Testing

Full suite (runs `build` first, then all tests under `test/`):

```bash
deno task test
```

Single file:

```bash
deno task build
deno test -A --no-lock test/<name>.test.tsx
```

## Linting and formatting

```bash
deno lint
deno task fmt    # dprint; same as: dprint fmt
```

## CI

GitHub Actions on `main` runs `deno lint` then `deno task test` (Deno 2.7). Match that locally before pushing.
